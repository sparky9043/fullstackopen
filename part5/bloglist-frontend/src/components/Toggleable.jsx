const Toggleable = ({ children }) => {
  return (
    <div>
      <div>

      </div>
      <div>
        {children}
      </div>
      <button>cancel</button>
    </div>
  )
}

export default Toggleable